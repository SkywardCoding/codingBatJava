// Given an array of ints, return the number of 9's in the array.

public int arrayCount9(int[] nums) {
  int result = 0;
  for(int number : nums) {
    if (number == 9) result++;
  }
  return result;
}
